# ReLax Tactical Board 作業記録

## 2026-07-31

- 作業対象を独立アプリ `/Users/oke/Developer/relax-tactical-board-private` に限定。
- 元の ReLax Board `/Users/oke/Developer/relax-board-dev-private` は読み取り確認のみ。既存の未追跡 `backups/` を含め変更していない。
- `codex/mobile-display-fullscreen` ブランチを作成。
- 変更前の `static/tactical_board/index.html`、`docs/index.html`、`README.md`、`IMPLEMENTATION_STATUS.md` を `backup/20260731_mobile_display_fullscreen/` に複製。
- 現行保存形式を確認。駒座標は相対値、描画座標は固定 viewBox 基準で、通常表示と全画面の切替に適した構造。
- 現行の保存・自動保存は状態オブジェクト全体を保持するため、`displaySettings` を状態へ追加し、古いデータには読込時の既定値を補う方針とした。
- 新規プレーの青10人・赤10人の背番号と名前を空欄化。既存保存データは読込時に値を文字列へ正規化するだけで、背番号・名前を保持する。
- ポジション編集を AT / MF / DF / G / FO / DMF / LMF / その他の選択式へ更新。選択肢外の既存値は「その他」欄へ復元する。
- 駒の見た目をスマートフォンで35pxへ小型化し、操作判定は48pxを維持。表示行数ごとのフォント調整と長い名前の省略を追加。
- `displaySettings`（ポジション、背番号、名前）を状態へ追加。初期値は ON / OFF / OFF。自動保存、保存済みプレー、Undo/Redo、印刷PDFへ状態ごと反映。
- Fullscreen APIとアプリ内疑似全画面を併用。`visualViewport`、`100dvh`、セーフエリアを考慮し、全画面ではヘッダー・左右パネル・通常操作バーを非表示化。
- 全画面の終了ボタン、呼び出し式ツールメニュー、移動・矢印・点線・ペン・追加・その他・Undo・Redoを実装。
- 390×844、844×390、1024×1366、1366×1024で確認。スマートフォン横画面の既存最小高さによるスクロールと縦横比崩れを修正。
- ブラウザ確認: 初期表示、編集、その他ポジション、表示3項目/全OFF、設定再読込、保存/読込、小型駒ドラッグ、全画面メニュー、矢印描画、全画面解除後の座標保持、Undo/Redo、PDF出力準備データ。
- 静的確認: JavaScript構文、旧version 1データの背番号/名前保持、旧データへの表示設定既定値追加、長い名前の省略。
- コミット `1bdb833` を独立アプリの `main` へ反映し、GitHub Pagesのビルド完了を確認。公開HTMLとService Worker v4をオンライン取得して新機能を確認。
- 公開URL: `https://tcz9mn4jh5-svg.github.io/relax-tactical-board-private/`
- 実機確認として iPhone Safari、ホーム画面追加版PWA、Apple Pencil、オフライン再起動を残す。

## 2026-07-31 DMF・フィールドモード・消しゴム追加修正

- 作業対象は引き続き独立アプリ `/Users/oke/Developer/relax-tactical-board-private` のみ。
- 元の ReLax Board は読み取り確認のみで、既存の未追跡 `backups/` を含め変更していない。
- `codex/dmf-half-eraser` ブランチを作成。
- 変更前HTML、Service Worker、TODO、WORKLOG、実装状況を `backup/20260731_dmf_half_eraser/` に複製。
- 現状調査で、フル／ハーフ切替、ハーフコート描画、相対座標保持、保存復元は既に実装済みと確認。
- 消しゴムは既に駒と描画線の双方へ対応し、削除操作は履歴へ入るためUndo可能と確認。
- 既存構造を維持し、旧ポジション表記とのDMF互換、新規プレー選択UI、全画面の消しゴム導線を追加する方針。
- 追加パレットと編集選択肢をDMFへ更新。旧保存データは読込時にDMFへ正規化し、背番号・名前など他の情報を保持する。
- 「新規」からフルフィールド／ハーフフィールドを選択できるモーダルを追加。初期配置リセット時は現在のフィールドモードを維持する。
- 全画面フローティングツールへ消しゴムを直接追加。消去した駒を通常画面・全画面のUndoで復元できることを確認。
- 矢印描画を消しゴムで削除し、Undoで復元できることを確認。
- ブラウザ確認: DMF追加、保存、再読込、PDF出力準備、フル／ハーフ新規作成、ハーフ比率0.98、フル比率1.82。
- レスポンシブ確認: 390×844、844×390、1024×1366、1366×1024。全サイズでページオーバーフローなし。
- コミット `8c90e28` を独立アプリの `main` へ反映。GitHub Pagesのビルド、Service Worker v5、公開版のDMF・新規ハーフ選択・全画面消しゴムをオンライン確認。

## 2026-07-31 消しゴムのタップ判定修正

- 線・矢印の消去が実機で反応しにくいとの報告を受けて再調査。
- 現行実装は表示線そのものだけを `pointer-events: stroke` の対象としており、細線ではスマートフォンの指で命中させにくいことを確認。
- `codex/eraser-hit-target` ブランチを作成。
- 変更前HTML、Service Worker、TODO、WORKLOGを `backup/20260731_eraser_hit_target/` に複製。
- 描画の見た目は維持し、各描画へ透明で太いタップ判定専用パスを重ねる方針。
- 各実線・点線・曲線・ペンへ最低32px幅の透明な判定専用パスを追加。表示パスはイベントを受けない構造へ分離。
- スマートフォン通常画面で、表示線から約10px外れた位置の指相当タップでも矢印が削除されることを確認。
- 全画面でも同じ判定で矢印をタップ削除できることを確認。
- 通常画面・全画面の双方で、削除後のUndoとRedoにより表示線と判定線が同時に復元・再削除されることを確認。
- PDFには透明な判定線を出力しない印刷スタイルを追加。
- コミット `9affaa8` を独立アプリの `main` へ反映。GitHub Pages、Service Worker v6、公開HTMLの32px判定パスをオンライン確認。
